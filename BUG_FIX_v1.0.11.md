# v1.0.11 版本问题修复说明

## 🐛 已修复的问题

### 1. 密码修改失败问题
**问题现象**：在登录页面点击修改密码，输入初始密码和两遍新密码后，提示"密码修改失败，请重试"

**原因分析**：
- 密码修改API调用时，服务器端未记录详细错误日志
- 容器运行时可能没有权限写入数据文件
- 前端错误提示不明确

**修复方案**：
- 增加了详细的服务器端日志记录
- 添加了更明确的错误提示信息
- 改进了错误处理机制

### 2. 数据未保存问题
**问题现象**：使用原始密码登录后，添加记录但数据目录中没有生成对应的yaml文件

**原因分析**：
- 数据保存API调用失败但前端没有明确提示
- 服务器端写入文件时缺少错误日志
- 数据目录权限可能存在问题

**修复方案**：
- 增加了数据保存的详细日志记录
- 添加了数据保存失败的明确提示
- 改进了数据持久化错误处理

## 🔧 技术改进

### 服务器端改进
1. **增强日志记录**：
   - 密码文件写入时记录详细日志
   - 数据文件写入时记录详细日志
   - 数据目录创建时记录详细日志

2. **改进错误处理**：
   - 捕获并记录具体的错误信息
   - 向前端返回更详细的错误描述

### 前端改进
1. **增强错误提示**：
   - 数据保存失败时显示具体错误信息
   - 密码修改失败时显示具体原因

2. **改进调试信息**：
   - 控制台输出更详细的保存操作日志
   - 记录API调用的详细信息

## 📦 部署说明

### 升级到 v1.0.11
```bash
# 停止当前容器
docker-compose down

# 拉取新版本镜像
docker-compose pull

# 启动新版本
docker-compose up -d
```

### 验证修复
1. **密码修改测试**：
   - 登录页面点击"修改密码"
   - 输入正确的旧密码和新密码
   - 应该成功修改并跳转到登录页面

2. **数据保存测试**：
   - 登录后添加一条新记录
   - 检查浏览器控制台是否有错误信息
   - 检查容器中的`/app/data`目录是否生成了数据文件

## 📁 数据目录结构
```
data/
├── password.json    # 密码文件
├── records.json     # 记录数据
└── giftbooks.json   # 礼薄数据
```

## 🛠 故障排除

如果仍然遇到问题，可以：

1. **检查容器日志**：
   ```bash
   docker logs liji
   ```

2. **检查数据目录权限**：
   ```bash
   docker exec liji ls -la /app/data
   ```

3. **检查浏览器控制台**：
   - 按F12打开开发者工具
   - 查看Console标签页的错误信息

4. **手动测试API**：
   ```bash
   # 测试密码状态
   curl http://localhost:3000/api/password/status
   
   # 测试数据保存
   curl -X POST http://localhost:3000/api/records \
        -H "Content-Type: application/json" \
        -d '[]'
   ```

## 📝 版本变更记录

- **v1.0.11** (2026-02-15)
  - 修复密码修改失败问题
  - 修复数据保存失败问题
  - 增强错误日志记录
  - 改进用户错误提示

- **v1.0.10** (2026-02-15)
  - 修复容器健康检查失败问题

- **v1.0.9** (2025-12-08)
  - 添加Excel导入导出功能

## 🆘 支持信息

如果问题仍然存在，请提供：
1. 容器日志输出
2. 浏览器控制台错误信息
3. 具体的操作步骤
4. 系统环境信息